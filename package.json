{
  "name": "encantar-project",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de entregas e beneficiários",
  "scripts": {
    "setup": "npm run install:all && npm run docker:config",
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ..",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "docker:setup": "docker-compose config && npm run install:all",
    "docker:start": "docker-compose down -v && docker-compose build && docker-compose up -d",
    "docker:stop": "docker-compose down",
    "docker:restart": "docker-compose restart",
    "docker:reset": "docker-compose down -v && docker-compose up -d postgres && sleep 10 && docker-compose up -d",
    "docker:clean": "docker-compose down -v --remove-orphans && docker system prune -f",
    "docker:logs": "docker-compose logs -f --tail=50",
    "docker:status": "docker-compose ps",
    "docker:config": "docker-compose config",
    "docker:dev": "docker-compose down && docker-compose up -d postgres",
    "db:migrate": "cd backend && npx prisma migrate deploy",
    "db:seed": "cd backend && npm run prisma:seed",
    "db:reset": "npm run db:migrate && npm run db:seed",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "cd backend && npm run lint",
    "lint:frontend": "cd frontend && npm run lint",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "health": "curl -f http://localhost:3001/health && curl -f http://localhost:3000",
    "update:all": "cd backend && npm update && cd ../frontend && npm update && cd .."
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "nodejs",
    "nextjs",
    "prisma",
    "docker",
    "postgresql",
    "typescript"
  ],
  "author": "Encantar Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}